/// Central API configuration.
/// Update [baseUrl] here to switch between environments.
class ApiConfig {
  ApiConfig._();

  static const String baseUrl = 'https://core-app-x3ok.onrender.com';
  static const Duration timeout = Duration(seconds: 45);
}
